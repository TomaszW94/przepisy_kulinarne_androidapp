<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.przepisyKulinarne">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ico_app"
        android:label="Przepisy kulinarne"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/HiddenTitleTheme">
        <activity android:name="com.przepisyKulinarne.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="RecipesListActivity"

            android:theme="@style/AppTheme"
            android:label="Lista przepisów">
        </activity>
        <activity
            android:name="RecipesActivity"
            android:label="@string/activity_sec"
         ></activity>
    </application>

</manifest>